/**< Test Common inherited functions */
#include "NGS++.h"
#include <iostream>
#include <string>
#include <fstream>
#include <vector>
#include <time.h>
#include <thread>
#include <random>
#include <string.h>
#include <time.h>
#include <gtest/gtest.h>


using namespace std;
using namespace NGS;


/**< applyOnAllChroms */

 TEST(BasicNGSEXP_applyOnAllChroms, VALID){
       ASSERT_TRUE(false);
 }
 TEST(BasicNGSEXP_applyOnAllChroms, EMPTY){
       ASSERT_TRUE(false);
 }


/*
 * Setters/Getters testing - start/end (positions)
 *	Valid cases:
 *		SETGETSTART
 *		SETGETEND
 *	Invalid cases:
 *		SETSTARTILLEGAL
 *		SETSENDILLEGAL
 */
TEST(uBasicNGSEXP_removeSpecificSites, VALIDCHR){
       ASSERT_TRUE(false);
 }
TEST(uBasicNGSEXP_removeSpecificSites, EMPTYCHR){
       ASSERT_TRUE(false);
 }
 TEST(uBasicNGSEXP_removeSpecificSites, NONECHR){
       ASSERT_TRUE(false);
 }


 /**< Accumulate */

TEST(uBasicNGSEXP_accumulateChromsInfo, VALID){
       ASSERT_TRUE(false);
 }
 TEST(uBasicNGSEXP_accumulateChromsInfo, NONECHR){
       ASSERT_TRUE(false);
 }

/**< ComputeOnAllChroms */

 TEST(uBasicNGSEXP_ComputeOnAllChroms, VALID){
       ASSERT_TRUE(false);
 }

 TEST(uBasicNGSEXP_ComputeOnAllChroms, NONECHR){
       ASSERT_TRUE(false);
 }

/**< ComputeOnOneChrom */

 TEST(uBasicNGSEXP_ComputeOnOneChrom, VALID){
       ASSERT_TRUE(false);
 }
 TEST(uBasicNGSEXP_ComputeOnOneChrom, INVALID){
       ASSERT_TRUE(false);
 }

 /**< getSpecificChroms */


 TEST(uBasicNGSEXP_getSpecificChroms, VALID){
       ASSERT_TRUE(false);
 }
 TEST(uBasicNGSEXP_getSpecificChroms, NONE){
       ASSERT_TRUE(false);
 }
  TEST(uBasicNGSEXP_getSpecificChroms, EMPTY){
       ASSERT_TRUE(false);
 }

/**<  applyOnOneChrom*/
 TEST(uBasicNGSEXP_applyOnOneChrom, VALID){
       ASSERT_TRUE(false);
 }
 TEST(uBasicNGSEXP_applyOnOneChrom, INVALID){
       ASSERT_TRUE(false);
 }
/**< applyOnSites */
 TEST(uBasicNGSEXP_applyOnSites, VALID){
       ASSERT_TRUE(false);
 }
  TEST(uBasicNGSEXP_applyOnSites, NONECHR){
       ASSERT_TRUE(false);
 }


